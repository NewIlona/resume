<div> 
    {{#with main}}
    <main>
        <section>
            <h2>{{> title data='My educations'big=true}}</h2>
           <ul>
            {{#each educations}}
              <li>
               {{name}}

                   {{#if point includeZero=true}} ({{point}}) {{/if}}
                   {{#if isTop}}  {{TOP}} {{/if}}
                 
                   {{#unless point}} 
                       {{#if point includeZero=true}}
                          (зараз вивчаємо)
                       {{/if}}
                   {{/unless}}

                   {{#unless point}} 
                       {{#unless point includeZero=true}}
                          (ще в процесі)
                       {{/unless}}
                   {{/unless}}
            </li>
            {{/each}}
        </ul>
        </section>

        <section>
              <h3>{{> title data='My certificates'}}</h3>
            <ul>
                {{#each certificates}}
             <li>
                {{name}}  ({{id}})
             </li>
             {{/each}}
            </ul>
         </section>
       </main>
    {{/with}}
    </div>